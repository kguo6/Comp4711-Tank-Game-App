class Achievement {
  constructor(title, description, kills, imageURL) {
    this.title = title;
    this.description = description;
    this.kills = kills;
    this.imageURL = imageURL;
  }
}

module.exports = Achievement;
